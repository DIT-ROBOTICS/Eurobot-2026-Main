/**:
  ros__parameters:
    robot_name: "Black"
    
    # Timing Configuration
    time_rate: 100          # Timer rate in microseconds (100 = 10kHz)
    game_time: 100          # Total game time in seconds
    sima_tick_threshold: 85 # Seconds before sima starts
    sima_game_over_trigger_sec: 99 # Seconds tell sima to stop
    group_num: 5            # Total number of groups (including index 0)
    
    # Plan Configuration
    default_plan: 21        # Default plan code when web service unavailable (format: XY, X=plan number, Y=team 0=yellow 1=blue)
    
    # BT Engine Configuration
    pkg_share_dir: "/home/main/eurobot-2026-main-ws/install/bt_core/share/bt_core"
    tree_name: "MainTree"   # Main behavior tree name
    json_file_path: "params/mission_sequence_black.json"  # Relative path from pkg_share_dir
    bt_xml_directory: "bt/"                          # Relative path from pkg_share_dir
    bt_tree_node_model: "bt/tree_node_model.xml"   # Relative path from pkg_share_dir
    
    # Dock Type Configuration
    normal_dock_type_y: "mission_dock_y_rush"  # Dock type for MISSION_DOCK_Y (case 0)
    normal_dock_type_x: "mission_dock_x_rush"  # Dock type for MISSION_DOCK_X (case 1)
    cam_dock_type_y: "mission_dock_cam_y"         # Dock type for CAM_DOCK_Y (case 2)
    cam_dock_type_x: "mission_dock_cam_x"         # Dock type for CAM_DOCK_X (case 3)
    
    # Dock Staging Dist of Each Side
    staging_dist_front: 0.205
    staging_dist_right: 0.205
    staging_dist_back: 0.205
    staging_dist_left: 0.205

    # Start Pose Configuration
    # Format: [x, y, yaw_in_radians]
    
    # Configuration for Blue Team match
    blue_team:
      start_pose: [2.7, 1.775, 0.0]
      
    # Configuration for Yellow Team match
    yellow_team:
      start_pose: [0.3, 1.775, 0.0]